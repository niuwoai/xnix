# Xnix unit presets baked into the image. Fedora 44's Plasma Login
# Manager drives the graphical login. The production Runtime D-Bus owner
# remains gated and must not be enabled as the development CLI wrapper.
enable plasmalogin.service
