#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/xnix/compatibility/compatibility_backend_lifecycle"

exit Xnix::Compatibility::CompatibilityBackendLifecycle::CLI.new(ARGV).run
